CREATE DATABASE RSA ON PRIMARY(
    NAME='RSA_DATA',
    FILENAME='E:\DBs\RSA_DATA.mdf',
    SIZE=10MB,
    MAXSIZE=UNLIMITED,
    FILEGROWTH=10%),
FILEGROUP RSA_INDEX(
    NAME='RSA_INDEX',
    FILENAME='E:\DBs\RSA_INDEX.ndf',
    SIZE=10MB,
    MAXSIZE=UNLIMITED,
    FILEGROWTH=10%)
LOG ON(
    NAME='translog',
    FILENAME='F:\DBLogs\translog.ldf',
    SIZE=10MB,
    MAXSIZE=UNLIMITED,
    FILEGROWTH=10% )
GO

-- Set recommended performance settings on the database
ALTER DATABASE [RSA] SET AUTO_SHRINK ON
GO
ALTER DATABASE [RSA] SET RECOVERY SIMPLE
GO

CHECKPOINT
GO